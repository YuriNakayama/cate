hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}_${model.type}

data:
  path: "path/to/your/data"

model:
  type: "example_model"
  hidden_size: 128

training:
  batch_size: 64
  num_epochs: 10

mlflow:
  experiment_name: "my_experiment"
  # tracking_uri: ""
